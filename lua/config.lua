M = {}

-- function M.config(cfg)
--     local default_config = {
--         shell = "sh",
--     }
--
--     if cfg == nil or cfg == {} then
--         cfg = default_config
--     end
--
--     return cfg
-- end

return M
